<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc 
		https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans 
		https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context 
		https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	
	<annotation-driven />

	<!-- 정적 리소스 경로 맵핑 -->
	<resources mapping="/resources/**" location="/resources/" />
	
	<!-- base-package의 패키지이름을 기준으로 스캔, 빈 자동생성, 애노테이션-와이어링 자동 연결  -->
	<context:component-scan base-package="com.mycompany.myapp." />
	
	
	<!-- 뷰 리졸버인데  JstlView는 JSTL을 사용한 자원용 뷰  
	아래에서 viewClass 속성에 지정한 JstlView는 JSTL을 사용한 자원용 뷰 이다.
		InternalResourceView의 자식 클래스로 스프링 메시지 자원 파일을 JSTL의
		포맷 태그에서 사용할 수 있도록 해 준다. 뷰에서 JSTL을 사용한다면 별도로 
		viewClass를 지정하지 않아도 ViewResolver는 JstlView를 반환한다.		
	-->
	<beans:bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver"
		p:viewClass="org.springframework.web.servlet.view.JstlView"
		p:prefix="/WEB-INF/index.jsp?body=views/"
		p:suffix=".jsp"/>
	
<!-- 	<view-controller path= "/insert" view-name= "insert" />
	<redirect-view-controller path= "/markList" redirect-url= "/markList"/> -->
	<view-controller path= "/" view-name= "redirect:/markList" />
	 	<redirect-view-controller path= "/index" redirect-url= "/markList"/>
		<!--  <redirect-view-controller path= "/kakaoForm" redirect-url= "/kakaoMap"/> -->
	<!--
	아래는 ContextRoot/write로 들어오는 요청을 위에서 view-controller에 설정한 /writeForm 으로 리다이렉트 하는 예 이다.
	-->


	
	
</beans:beans>
